@props(['paginator'])
@if($paginator->hasPages())<nav class="flex items-center justify-between gap-3" aria-label="Navigasi halaman"><span class="text-xs text-slate-500">Halaman {{ $paginator->currentPage() }} dari {{ $paginator->lastPage() }}</span><div class="flex gap-2">@if($paginator->onFirstPage())<span class="rounded-lg border border-slate-200 px-3 py-2 text-xs text-slate-400">Sebelumnya</span>@else<a href="{{ $paginator->previousPageUrl() }}" class="rounded-lg border border-slate-200 px-3 py-2 text-xs font-semibold text-primary hover:bg-primary/5">Sebelumnya</a>@endif @if($paginator->hasMorePages())<a href="{{ $paginator->nextPageUrl() }}" class="rounded-lg bg-primary px-3 py-2 text-xs font-semibold text-white hover:bg-primary-dark">Berikutnya</a>@else<span class="rounded-lg border border-slate-200 px-3 py-2 text-xs text-slate-400">Berikutnya</span>@endif</div></nav>@endif

